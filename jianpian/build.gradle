plugins {
    id 'com.android.library'
}

android {
    namespace 'com.p2p'

    compileSdk {
        version = release(36)
    }

    defaultConfig {
        minSdk 24
        targetSdk 36
    }
}

dependencies {
    implementation project(':catvod')
}